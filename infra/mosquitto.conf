# Mosquitto config for Conduit.
# No broker auth — the network is secured by Tailscale / the LAN.

persistence true
persistence_location /mosquitto/data/

# Server-to-server / device TCP listener
listener 1883
protocol mqtt

# Browser (player) WebSocket listener
listener 9001
protocol websockets

allow_anonymous true
